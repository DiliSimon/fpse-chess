(library
  (name board)
  (modules board)
  (libraries 
    core)
  (preprocess (pps bisect_ppx ppx_jane))
)

(library
  (name bot)
  (modules bot)
  (libraries 
    core
    board)
  (preprocess (pps bisect_ppx ppx_jane))
)